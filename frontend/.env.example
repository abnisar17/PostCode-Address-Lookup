VITE_API_BASE_URL=http://localhost:8000
# Optional: API key sent as X-API-Key when the backend has REQUIRE_API_KEY=true.
# Baked into the client bundle at build time (public — use a dedicated,
# revocable key minted from the admin dashboard, not a high-privilege one).
VITE_API_KEY=
